# VoteGuard Nexus Frontend Scaffold

## Install

```powershell
cd frontend
npm install
```

## Run

```powershell
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` to the backend host when the API is ready.
