# Environment configuration for Smart Voter Management and EVM System
# Copy to .env and adjust paths; do NOT commit real secrets.

# Encrypted vote storage
VOTE_STORAGE_PATH=./votes.json
FERNET_KEY_PATH=./key.key

# Audit logging
AUDIT_LOG_PATH=./audit_log.enc

# Blockchain client (placeholder)
WEB3_PROVIDER_URL=http://127.0.0.1:8545
CONTRACT_ADDRESS=0xYourContractAddress

# Optional: Provide a base64 Fernet key directly (overrides FERNET_KEY_PATH)
# FERNET_KEY=base64-encoded-fernet-key
